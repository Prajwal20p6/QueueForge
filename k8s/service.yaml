apiVersion: v1
kind: Service
metadata:
  name: queueforge-service
  namespace: queueforge
  labels:
    app: queueforge-api
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: queueforge-api
  sessionAffinity: None
