replicaCount: 2

resources:
  limits:
    cpu: 300m
    memory: 768Mi
  requests:
    cpu: 80m
    memory: 200Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 75
