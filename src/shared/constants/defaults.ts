/**
 * Standard configuration threshold and page defaults for QueueForge
 */
export const DEFAULT_MAX_RETRIES = 5;
export const DEFAULT_BACKOFF_BASE = 2; // base for exponential backoff calculations
export const DEFAULT_TTL_HOURS = 24;
export const DEFAULT_PAGE_SIZE = 50;
export const MAX_PAGE_SIZE = 1000;
